

问题：
系统突然从 (config-if)# 模式 退出到 # 模式，怎么办？
问题分析：系统突然中断，导致从前一个模式退出到特权模式，如果当前要输入的命令必须在(config-if)模式下输入，那么需要重新进入(config-if)#模式。
解决办法：具体命令
#conf t
(config)#inter f*    //指向你需要配置的端口
(config-if)#



问题截图：

系统提示：Translating ……domain server（255.255.255.255）
问题分析：非法输入
解决办法：系统因操作失误死机，输入ctrl+shift+6。


问题：系统显示Press RETURN to get started!然后没有进入任何输入模式
问题分析：系统等待用户点击回车键。
解决办法：按回车键（Enter）即可。


问题截图：

问题识别：错误提示：%Invalid input detected at '^' marker
问题分析：非法输入
解决办法：检查输入模式（不同的模式下允许输入的命令是不同的）、命令格式（是否缺少空格）、是否存在拼写错误。
